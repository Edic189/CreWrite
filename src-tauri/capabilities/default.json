{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capabilities for the CreWrite main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-destroy",
    "dialog:default",
    "dialog:allow-open",
    "opener:default",
    "opener:allow-open-url"
  ]
}
